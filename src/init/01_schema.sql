-- ============================================================
-- PeopleCore DEV — cria o banco e garante o charset correto
-- ============================================================
CREATE DATABASE IF NOT EXISTS peoplecore_dev
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE peoplecore_dev;